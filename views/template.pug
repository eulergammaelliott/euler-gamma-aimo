mixin nav
  a(href="/") AI-MO Prize
  a(href="/about") About
  a(href="/supporters") Supporters
  a.primary(href="/get-involved") Get Involved

doctype html
html(lang="en")
  head
    title AI-MO Prize
    link(rel="icon", href="/favicon.png")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    meta(name="description", content="XTX Markets is launching a new $10mn challenge fund, the Artificial Intelligence Mathematical Olympiad Prize (AI-MO Prize). The fund intends to spur the development of AI models that can reason mathematically, leading to the creation of a publicly-shared AI model capable of winning a gold medal in the International Mathematical Olympiad (IMO).")
    meta(property="og:image", itemprop="image", content="https://aimoprize.com/banner-sq.png")
    meta(property="og:image:type", content="image/png")
    meta(property="og:image:width", content="300")
    meta(property="og:image:height", content="300")
    meta(property="og:image", itemprop="image", content="https://aimoprize.com/banner.png")
    meta(property="og:image:type", content="image/png")
    meta(property="og:image:width", content="1200")
    meta(property="og:image:height", content="600")

    meta(property="og:title", content="AI-MO Prize")
    meta(property="og:type", content="website")
    meta(property="twitter:card", content="summary_large_image")
    meta(property="twitter:creator", content="@aimoprize")
    meta(property="og:description", content="XTX Markets is launching a new $10mn challenge fund, the Artificial Intelligence Mathematical Olympiad Prize (AI-MO Prize). The fund intends to spur the development of AI models that can reason mathematically, leading to the creation of a publicly-shared AI model capable of winning a gold medal in the International Mathematical Olympiad (IMO).")
    meta(property="og:site_name", content="AI-MO Prize")
    style
      include ./style.css
    script(src="/turbolinks.js", defer)
  body
    div.bg(id="bg", data-turbolinks-permanent)
      script(data-turbolinks-eval="false").
        const tileSz = 60;
        const circleSz = 42;
        const bg = document.querySelector('.bg');

        function animate(){
          for(let i=0; i<window.innerWidth/400; i++){
            let rect  = bg.getBoundingClientRect();
            let x = Math.floor(Math.random()*(rect.width/tileSz))*tileSz;
            let y = Math.floor(Math.random()*(rect.height/tileSz))*tileSz;
            place(x,y);
          }
          setTimeout(animate, 4000);
        }

        function place(x, y){
          let el = document.createElement('div');
          let col = '#6ca2ad';
          el.setAttribute('style', `width: ${circleSz}px; height: ${circleSz}px; left:${x}px; top:${y}px; border-radius:100%; background:#6ca2ad; position:absolute; opacity:0; transition:opacity 500ms linear`);
          bg.appendChild(el);
          setTimeout(() => {
            el.style.opacity = .2 + Math.random();
          },100);
          setTimeout(() => {
            el.style.opacity = 0;
            setTimeout(() => {
              bg.removeChild(el);
            }, 1000);
          }, 15000);
        }

        animate();

    div.content
      details.nav-mobile
        summary Menu
        +nav
      div.nav-desktop
        +nav
      block content
      div.footer
        include:markdown-it ../content/footer.md